# Bitget Agent Hub — unified trading account (UTA / v3). Paper trading only in this repo.
BITGET_API_KEY=
BITGET_SECRET_KEY=
BITGET_PASSPHRASE=

# Optional: override symbols after Hub probe (defaults come from mandate.json)
# BITGET_RTOKEN_SYMBOL=
# BITGET_PERP_SYMBOL=

# LLM reason string channel (optional, one line, never computes anything)
LLM_REASON=